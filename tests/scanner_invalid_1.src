/* this is a comment this is a comment

this

is a /* comment */ /* this /* is */  a*/

comment */

program program_name is

  global integer 22foo[100];
  global float var_name_1738[500];
  global __ char c +=;
  string string_variable $$;
  bool #;
  integer an_integer_var;
  float %;
  @
  procedure do$things(string~ s in~, integer x[100] out, float f inout)~
    integer a;
    bool b{100};
  begin
    a := 0;
    for{a := a + 1;a == 4 + b[0] + b[2])
        putInteger(@);
        putBool(b[&]);
        if(4a < 100b | (b[0r] == b[1_]))then
        else
        endif;
    end for
  end procedure

begin

  foo[9] := 1/33 + var_name_1738[0_0_0] - (3.222f +' 23.4332/32.2 + foo[0']*foo[2]);
  string_variable = "this is a test" : ?;
  string_variable = "testing" : ;
  do_things("test",foo,f);
  do_things(string_variable,foo,f);

  if(true == false & false != true)then
    // this is a comment
    if(an_integer_var > foo[9] & b != false)then
      // this is another comment
      foo[3] := 3 + 4.5;
      foo[4] := 7/7/7*7*55*234.345*an_integer_variable*foo[4];
      foo := foo + 33;
    end if
  else
    putString("THIS IS A TEST"0u);
    putChar("x");
    @this77 := 3;
    putInteger(33333);
    for(x = 4)
      33333test := "7777777;
      ok := ;
    end for
  end if

  putString("hello, world");

$4 %d foobarspam

end program.
